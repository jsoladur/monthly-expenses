# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.3.4] - 2026-08-25

### Fixed
- Incomes screen responsive layout: split category and name fields into separate rows in both add and edit forms to prevent horizontal overflow on mobile devices.
- Incomes row display: applied consistent layout pattern with amounts getting more space (`whitespace-nowrap`, `shrink-0`) and name/category wrapping properly (`truncate`, `min-w-0`).

## [0.3.3] - 2026-08-25

### Changed
- Actuals edit form: category select now occupies full row, name and observations on separate rows for better mobile responsiveness.
- Row display layout: amounts now get more space with `whitespace-nowrap` and `shrink-0`, while name/observations wrap with `truncate` and `min-w-0`. Applied consistently across actuals, reserved lines, estimated reserved lines, and committed reserved rows.
- Stats screen: reordered sections so "Expenses by category" appears first, followed by "Distribution".
- Shortened "Committed reserved lines" label to "Committed" (EN) / "Comprometidas" (ES) in the actuals screen collapsible section.

## [0.3.2] - 2026-08-25

### Fixed
- Form layout overflow on mobile devices: added `flex-wrap` and `min-w-0` to form inputs in templates and annuals screens to prevent horizontal overflow on small screens.
- Added `overflow-x-hidden` to `main` and `section` elements in globals.css to ensure consistent width constraints across all screens.

## [0.3.1] - 2026-08-25

### Changed
- Renamed "Templates" to "Fixed expenses" (English) / "Gastos fijos" (Spanish) throughout the UI for better intuitiveness. The term "Fixed expenses" more clearly describes what these items are (recurring planned expenses) rather than the technical term "Templates".
- Navigation menu item renamed to "Fixed" (English) / "Fijos" (Spanish) with a DollarSign icon for better visual recognition.
- Updated all related UI text: page titles, form labels, help text, empty states, validation messages, and warnings to reflect the new terminology.
- Updated origin labels from "Cloned from template" to "Cloned from fixed expense" (English) / "Copiada de gasto fijo" (Spanish).

## [0.3.0] - 2026-08-25

### Added
- Pass to actual for estimated reserved lines: users can now move estimated reserved lines to actuals, same as committed lines. The undo functionality works identically — only allowed while the actual has not been edited. Migration 0004 adds `converted_line_kind` column to `month_actual_expense` to properly restore the original line kind on undo.

### Changed
- PRD §7.5 updated to allow both committed and estimated lines to be passed to actuals (previously committed-only).
- UC-10 documentation updated to reflect the extension.
- Help text updated to remove the note that estimates cannot be moved.

### Removed
- `EstimatedLineCannotPassError` no longer thrown — estimated lines can now be passed to actuals.
- `estimatedLineCannotPass` error code removed from pass-to-actual actions.
- `estimatedLineCannotPassToActual` i18n key removed from both locales.

## [0.2.1] - 2026-08-25

### Added
- Optional amount field in annuals: users can now set a reference amount when creating or editing an annual. The amount is displayed in the annual row and in the annual reminder cards on the month workspace. Migration 0003 adds the `amount` column (numeric(14,2), nullable) to the `annual` table.

### Changed
- Removed `annuals.help` text from the annuals management screen (no longer needed).

## [0.2.0] - 2026-08-25

### Added
- Annual reminders (UC-14): new catalog of annual recurring expenses with charge month tracking. Users can add, edit, deactivate and reactivate annuals. Annual reminder cards appear on the home page when an annual's charge month matches the current month, with collapsible accordion and warning styling.
- Icon-based action buttons with tooltips across all list screens (categories, templates, annuals, incomes, reserved lines, estimated reserved lines, actuals). Text labels replaced by compact icons (Pencil, Trash2, CircleOff, RotateCcw, ArrowRightCircle, Undo2, Check, X) with CSS tooltips on hover, saving horizontal space on mobile.

### Changed
- Navigation: History replaced by Annuals in the main navigation. Menu order is now Home, Templates, Annuals, Categories, Settings.
- Currency display changed from ISO codes (EUR, USD) to symbols (€, $, etc.) throughout the app.
- Annual reminder cards redesigned with warning color scheme, collapsible accordion layout, and removed duplicate name display.
- Past month warning message updated to "ATTENTION: This month is not the current calendar month."

### Fixed
- Fixed Server Component → Client Component error in annual reminder cards by removing onQuickAdd prop.
- Removed orphan i18n keys: annuals.help text, and confirmed history/quickAdd keys were unused after navigation and reminder card changes.

## [0.1.9] - 2026-08-25

### Added
- Observations field in template add and edit forms: users can now add optional notes to templates. The observations are displayed in the template row view.
- Distribution charts accordion on templates page: collapsed-by-default accordion with two pie charts (percentage and absolute values) showing expense distribution by category. Moved after the Total monthly expenses banner for better visual hierarchy.

### Changed
- Reduced legend font size in all pie charts (templates and month stats pages) for better visual balance.
- Stats page "Expenses by category" pie charts now include all expenses: actuals + committed reserved lines + estimated reserved lines, providing a complete picture of monthly expenses.
- Past month warning message changed to "ATTENTION: This month is not the current calendar month." (removed "Changes are allowed" suffix).
- Moved "New month-only line" form to appear before the reserved lines tabs for better UX.

### Fixed
- Fixed i18n key resolution for `month_only` origin: added `toCamelCase` helper to convert snake_case to camelCase so `reservedLines.origin.month_only` correctly resolves to `reservedLines.origin.monthOnly`. Applied fix in reserved-lines-screen.tsx, actuals-screen.tsx, and estimated-reserved-lines-screen.tsx.

## [0.1.8] - 2026-08-25

### Changed
- Reduced legend font size in pie charts (templates and month stats pages) for better visual balance.

## [0.1.7] - 2026-08-25

### Added
- Distribution charts accordion on templates page: collapsed-by-default accordion with two pie charts (percentage and absolute values) showing expense distribution by category, matching the month stats page styling.

## [0.1.6] - 2026-08-25

### Fixed
- AmountInput validation classifier: bare integers (e.g. `100`) are now classified as `incomplete` (shows a hint) rather than `ok`, matching the test expectation that partial typing should not be considered fully valid. The blur normalization still auto-appends `.00` to complete the wire format.

## [0.1.5] - 2026-08-25

### Added
- Observations field in template add and edit forms: users can now add optional notes to templates (the DB column already existed; the UI was missing). The add form (`add-template-form.tsx`) and the inline edit form (`template-row.tsx`) both include a text input for observations (max 500 chars), and the template row view displays observations when present.

### Changed
- AmountInput auto-appends `.00` on blur for integer values: typing `100` and leaving the field now normalizes to `100.00` automatically, matching the wire format. Partial decimals like `100.5` are also completed to `100.50`. The validation classifier now treats bare integers as valid so no error flashes while typing.

## [0.1.4] - 2026-08-24

### Fixed
- Google sign-in still failing for browsers that had the pre-0.1.3 service worker registered: navigation preload is a registration-level flag that SURVIVES service-worker updates, so the fixed worker inherited the enabled preload from older versions and the OAuth callback could still reach the server twice (`invalid_grant` → `/403`). The service worker now explicitly `disable()`s navigation preload on `activate` (first update past 0.1.3 turns it off for good), and `public/sw.js` is regenerated — the shipped 0.1.3 artifact still contained the buggy `navigationPreload: true` build because dev builds skip the Serwist plugin.

## [0.1.3] - 2026-08-24

### Fixed
- Google sign-in failing with `invalid_grant` ("Access Denied") in browsers with a registered service worker: `navigationPreload` is now disabled in the Serwist service worker (`src/app/sw.ts`). With it enabled, a service-worker lifecycle race could make the OAuth callback reach the server twice, consuming the single-use Google authorization code twice and breaking sign-in for users with an active Google session (serwist/serwist#150).

## [0.1.2] - 2026-08-24

### Added
- GitHub Actions CI/CD pipeline: reusable `common.yml` workflow with `lint`, `tests`, and `deploy` jobs; `main.yml` triggers on `main` branch and `v*` tags with Docker push enabled; `non-main-ci.yml` triggers on non-main branches and PRs with build-only (no push). Version extracted from `package.json` for Docker image tagging.
- Production `deploy/docker-compose.yml` with `pull_policy: always`, postgres 16-alpine with bind-mounted `./data/postgres` volume, healthcheck-gated `depends_on`, and app service pulling from Docker Hub. Accompanied by `deploy/.env.example` template.
- Real 512x512 PWA icons (`icon-512.png`, `icon-maskable-512.png`) generated from the app logo, replacing the solid-color placeholders.

### Fixed
- PWA installability: manifest now uses relative `start_url: "/"` and `scope: "/"` (previously absolute URLs from `NEXT_PUBLIC_APP_URL`), adds `id: "/"` field, and includes required 512x512 icon entries. Removed reference to non-existent `favicon.ico` from root layout metadata.
- `.gitignore` pattern `Icon?` (macOS Finder icon) was matching `public/icons/icon-512.png` and `icon-maskable-512.png` on case-insensitive filesystems, preventing them from being committed. Anchored to `/Icon?` (root-only).
- Dockerfile build stage no longer runs database migrations (`pnpm run build` → `npx next build --webpack`), fixing CI/Docker builds that failed with `DATABASE_URL is not set`.

### Added
- Theme preference (dark/light/auto): users can now choose their preferred theme in Settings. New `theme_preference` enum (`auto` / `light` / `dark`) with `profile_settings.theme` column defaulting to `auto`. Auto mode follows the system's `prefers-color-scheme` setting. Theme provider applies the `dark` class to `<html>` and persists the resolved theme to a cookie for instant application on subsequent loads. Inline script in root layout prevents flash of wrong theme. Settings page includes a theme selector alongside the existing currency picker. i18n keys added under `settings.theme*` in both locales.
- PWA install (UC-12): app is now installable on Android Chrome and iOS Safari (PRD UC-04, C11). Serwist service worker precaches the app shell only — no runtime caching of API/data responses (online-only, ARCH §7 / ADR-7). Web manifest (`src/app/manifest.ts`) serves `start_url` / `scope` from `NEXT_PUBLIC_APP_URL`, `display: 'standalone'`, and icons at 192 / 512 / maskable-512 in `public/icons/`. New client component `src/components/pwa-install-prompt.tsx` shows a permanent install affordance on the home page: captures `beforeinstallprompt` on Android Chrome to surface an install button, and shows iOS-specific "Add to Home Screen" instructions on Safari. Detects standalone mode via `display-mode: standalone` media query + iOS `navigator.standalone` and hides once installed. Dismissable via an X button. i18n keys expanded under `pwa.install.*` in both locales.

### Added
- Month summary, savings & warnings (UC-11): the workspace header now shows the potential-savings number on day one, per-category overspend warnings, and the past-month banner (PRD §7.1 / §7.4 / §7.7 / §19, UC-13, §15 #5, #6, #7, #8, #13, #14, #19). New service `src/server/services/summary.ts` owns the read-only summary algebra (ADR-6 / ARCH §5 rule 3 — services own domain rules; pure reads, no transactions). `getMonthSummary(userId, monthId)` returns `{ incomesTotal, actualsTotal, reservedRemainingTotal, potentialSavings }` in INTEGER CENTS — `potential_savings = Σ month_income.amount − (Σ month_actual_expense.amount + Σ month_fixed_line.remaining_amount)` (PRD §7.1, #5–#8, #14). The three money rows are pulled with three parallel `db.select()` calls against `month_income.month_id` / `month_actual_expense.month_id` / `month_fixed_line.month_id` and summed via `parseAmount` + `sumCents` (ADR-5, ARCH §8). Negatives are first-class (PRD §7.6): `Actual −20` raises savings by 20 (#14). Hard-deleted rows are excluded by virtue of being gone (PRD C15). `getOverspendWarnings(userId, monthId)` computes per-category overspend: LEFT = `SUM(amount)` of `month_actual_expense` grouped by category (one PG `SUM()` aggregate, returns one row per category that has any tickets); RIGHT = `Σ amount` of ACTIVE ESTIMATED templates for that category. The baseline reuses the existing `listActiveTemplates` helper so the warning baseline is sourced from the exact clone-source query UC-06 uses at month creation (PRD §7.4 vs §7.8). A category with NO active estimated templates — including those with only committed templates — is excluded from warnings via the `categoriesWithEstimatedPlan.has(categoryId)` guard (PRD §7.4 / C18 / #19). Warn when `actualsTotal − estimatedTemplateTotal > 0`. The MONTH'S remaining is NEVER used for this baseline (PRD §7.4: "Do not compare actuals to the month remaining box"). `isPastMonth(year, month, now)` returns true whenever the open month is not the current calendar month (PRD §7.7 / C8 / UC-13); `now` is injected so the helper is unit-testable without monkey-patching `Date`. Tenancy: every read resolves `month.user_id = userId` via `findMonthById` BEFORE the aggregation runs (PRD §5.1, UC-17); a tenant calling with a `monthId` they don't own gets `MonthNotFoundError` — same contract every other service exposes (no `userId`-less repository call exists, ARCH §10). New RSC components: `summary-block.tsx` (income / actuals / reserved / savings hero card with `savingsHelp` PRD §19 copy), `overspend-badge.tsx` (inline notification with PRD §19 copy plus `{plan}` / `{overrun}` template args), `past-month-banner.tsx` (PRD §7.7 banner with "Changes are allowed." reassurance). Workspace page.tsx now loads `summary` + `overspendWarnings` in parallel with the existing workspace fetch via `Promise.all`, then conditionally renders the past-month banner (above the summary) and the summary block (below the header). The overspend warnings are passed into BOTH the reserved-lines screen and the actuals screen; each screen de-dupes by `categoryId` and renders one `OverspendBadge` per overspending category above its row list — the badge never blocks (PRD §7.4). i18n bundles extended with `months.summary.{negativeNote, pastMonthBanner}` + `warnings.{overspendPlan, overspendOver}` in BOTH `en` and `es`; parity test in `tests/unit/i18n-messages.test.ts` catches drift between locales. PRD §15 #5 (Mortgage 800 + Groceries 400 + Income 2000 → savings 800), #6 (Groceries ticket 50 → savings 750, double-count by design PRD §7.3), #7 (decrease Groceries remaining 400→350 after the #6 ticket → savings back to 800), #8 (pass mortgage to actual → savings STILL 800, money in exactly one place PRD §7.2), #13 (past-month banner visible, edits still allowed), #14 (Actual −20 raises savings by 20), #19 (Food templates 400+50, actuals 500 → warning; month remaining is IGNORED for the warning baseline; categories with only committed templates produce NO warning; inactive estimated templates are excluded from the baseline), plus tenancy isolation + hard-delete exclusion (PRD C15) + negative actuals algebra, all covered by `tests/integration/summary.test.ts` (19 cases) and `tests/e2e/summary.spec.ts` (6 cases — #5, #8, #19, no-warning edge, #13 past-month, Spanish variant).
- Pass-to-actual & undo (UC-10): one-tap cut-paste of a COMMITTED reserved line into actuals, with undo allowed only while the created actual has not been edited (PRD §7.5 / UC-12 / §7.2; §15 #8, #9, #10, #12). New service `src/server/services/pass-to-actual.ts` owns `passToActual` + `undoPassToActual`. Each operation runs in ONE `db.transaction` (ARCH §5, PRD §16): pass inserts a new `month_actual_expense` (amount = line.remaining_amount; copies category_id / name / observations verbatim; `converted_from_line_id = line.id`, `converted_line_original_amount = line.original_amount`, `converted_line_origin = line.origin`, `edited_after_conversion = false`) and HARD-deletes the source line in the same tx — so the money exists in exactly one place after the move (PRD §7.2, #8). Estimated lines throw `EstimatedLineCannotPassError` and the tx never opens (PRD §7.5, #12). Undo is gated on `converted_from_line_id IS NOT NULL AND edited_after_conversion = false` (PRD §7.5, #9, #10); the re-insert of `month_fixed_line` REUSES `converted_from_line_id` as the row id (the row was hard-deleted by pass, so the id is free for re-use) with `remaining_amount = actual.amount`, `original_amount = converted_line_original_amount`, `kind = 'committed'`, `origin = converted_line_origin`; the actual is hard-deleted in the same tx. Both ops join on `month.user_id` for tenancy (PRD §5.1) — no `userId`-less repository call exists (ARCH §10). Both ops NEVER mutate a `template` row (PRD §7.8) — verified by `tests/integration/pass-to-actual.test.ts` ("pass preserves the source template" + "undo preserves the source template"). New server actions `passToActualAction` / `undoPassToActualAction` in `src/actions/pass-to-actual.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/months/<year>/<month>', 'page')` pattern (ARCH §5 rule 3) and translate domain errors into `PassToActualActionError = "monthLineNotFound" | "estimatedLineCannotPass" | "actualNotFound" | "notUndoable" | "undoForbiddenAfterEdit" | "validation"`. The UI renders a "Pass to actual" button ONLY on committed rows in `ReservedLinesScreen.RowActions` (estimated rows simply have no such affordance — matching the spec) with the PRD §19 help copy "Move this commitment to actual spend. Estimates cannot be moved." surfaced under the committed section heading so the lack of a button on estimated lines is self-explanatory. The actuals block mirrors that: `ActualsScreen.RowActions` renders "Undo pass" ONLY when `convertedFromLineId !== null && !editedAfterConversion` (UC-08 already flips that flag on every edit so undo is suppressed the moment the user touches a converted ticket, PRD §7.5). The `ActualRowData` shape grew two new fields (`convertedFromLineId`, `editedAfterConversion`) so the RSC serialization wires the gate end-to-end; `page.tsx` populates them from `workspace.actuals`. i18n bundles extended with `reservedLines.actions.{passToActual, passToActualHelp, confirmPassToActual}`, `actuals.actions.{undoPass, confirmUndoPass}`, and `validation.{estimatedLineCannotPassToActual, cannotUndoPass, notUndoable}`; parity test in `tests/unit/i18n-messages.test.ts` catches drift between locales. PRD §15 #8 (Mortgage 800 → savings unchanged at 800, money only in actuals), #9 (undo on unedited → line back), #10 (edit after pass → undo rejected), #12 (estimated line rejects), plus the atomicity contract (a simulated mid-tx failure rolls back BOTH writes — `vi.spyOn` on the actual repo's `insertActual` and on the reserved-line repo's `insertMonthLine`) and two-tenant isolation, covered by `tests/integration/pass-to-actual.test.ts` (17 cases) and `tests/e2e/pass-to-actual.spec.ts` (chromium + mobile-safari, 3 cases — pass→undo round-trip with DB row-count check, no pass-to-actual button on estimated lines, Spanish variant with `Pasar a gastos reales` / `Deshacer pase`).
- Reserved lines: edit remaining & month-only lines (UC-09): manual `remaining_amount` edits on cloned or one-off lines plus a "new month-only line" flow that lives only on the current instance (PRD UC-11 / UC-18; §6.6; §7.3; §7.8). New `userId`-first repository `src/server/repositories/reserved-line.ts` (`insertMonthLine` / `findMonthLineById` / `updateMonthLineRemaining` / `deleteMonthLine`); `month_fixed_line` has no direct `user_id` column so every read/write joins on `month` with `month.user_id = userId` (PRD §5.1). `updateMonthLineRemaining` is a single-field patch on `remaining_amount` only — `original_amount`, `kind`, `origin`, `category_id`, `name` and `observations` are immutable for the life of the row (PRD §6.6); the repo signature enforces the rule so the service cannot accidentally rewrite history. `deleteMonthLine` uses a raw `sql` template (Drizzle's PG delete builder doesn't expose `.using()`) so the WHERE clause enforces tenancy in a single statement — PRD C15 / §13 hard delete on month-scoped money. New service `src/server/services/reserved-lines.ts` owns `updateRemainingAmount` / `addMonthOnlyLine` / `deleteMonthLine`. Domain rules: manual remaining edits are allowed for BOTH `committed` AND `estimated` kinds (PRD §6.6 / §7.4) and accept zero and negative values (PRD §7.4 / §7.6); month-only lines insert with `origin = 'month_only'` and `remaining_amount = original_amount = amount` (PRD §6.6) and the target category must be ACTIVE EXPENSE-kind (IncomeCategoryError / InactiveCategoryError / CategoryNotFoundError — the latter reused from the categories service so the action layer maps one i18n key across both surfaces). The parent month is resolved via `findMonthById` BEFORE category validation so a missing month surfaces as `MonthNotFoundError` first (PRD §5.1). Editing a remaining NEVER creates an actual ticket (PRD §7.3, UC-11) and NEVER mutates a template row (PRD §6.6 / §7.8) — implicit because the service only touches `month_fixed_line`, verified by `tests/integration/reserved-lines.test.ts` ("deleting a line NEVER touches the source template" + "reserved-line CRUD never creates or mutates a template row"). Server actions `updateRemainingAmountAction` / `addMonthOnlyLineAction` / `deleteMonthLineAction` in `src/actions/reserved-lines.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/months/<year>/<month>', 'page')` pattern (ARCH §5 rule 3) and translate domain errors into `ReservedLineActionError = "incomeCategory" | "inactiveCategory" | "categoryNotFound" | "monthLineNotFound" | "monthNotFound" | "amountFormat" | "validation"`. The UI replaces the UC-06 "coming next" placeholders with a real `ReservedLinesScreen` client (`src/app/[locale]/months/[year]/[month]/reserved-lines-screen.tsx`): committed/estimated sections in PRD §10 order, each row showing the current `remaining` with the cloned `original` underneath when they differ (so the manual edit is visible), an inline "Edit remaining" form bound to the `AmountInput`, an "X" delete with `window.confirm`, and an "Add month-only line" form (category + kind + name + amount) that shows a "create an active expense category first" message when the picker would be empty (PRD §6.6). Rows are pre-grouped by `kind` in the RSC and ordered with `cloned` before `month_only` so the snapshot reads as "what the template said" followed by "what I added on top" (PRD §7.8 / §6.6). The PRD §19 "Tickets do not reduce this number…" copy surfaces on the edit form so the manual-remaining affordance is obvious. i18n bundles extended with `reservedLines.*` (title / kind / origin / actions / category / amount / remaining / remainingHelp / noCommitted / noEstimated / historicalInactiveNote / noActiveCategories) + `validation.reservedLineNotFound`; parity test in `tests/unit/i18n-messages.test.ts` catches drift between locales. PRD scenarios #7 (groceries 400 → 350 raises savings to 800), #17 (one-off August line does not appear in September), #18 (August remaining edit does not leak into September), plus the §6.6 / §7.3 / §7.8 / C15 contract (edit never creates an actual, hard delete leaves templates intact, two-tenant isolation) covered by `tests/integration/reserved-lines.test.ts` (17 cases).
- Actual expenses (UC-08): real expense tickets per month with add / edit / hard-delete (PRD UC-10 / UC-16; §6.7; C13 / C15; §7.2 / §7.3). New `userId`-first repository `src/server/repositories/actual.ts` (`insertActual` / `findActualById` / `updateActual` / `deleteActual` / `listActualsForMonthForUser`); `month_actual_expense` has no direct `user_id` column so every read/write joins on `month` with `month.user_id = userId` (PRD §5.1) — same tenancy pattern as the income repo. `update` uses PG `UPDATE ... FROM month WHERE month.user_id = userId` in one statement; `delete` uses a raw `sql` template (Drizzle's PG delete builder doesn't expose `.using()`) so the WHERE clause enforces tenancy in a single statement — PRD C15 / §13 hard delete on month-scoped money rows. The repo's `updateActual` patch type intentionally omits `edited_after_conversion` — the repo signature ENFORCES `edited_after_conversion = true` on every edit (PRD §7.5 undo gate) so the service cannot accidentally forget it; that flag is the input UC-10 keys off for "undo allowed only while unedited". New service `src/server/services/actuals.ts` owns `addActual` / `editActual` / `deleteActual` / `listActualsForMonth`. Domain rules: the target category must be an ACTIVE expense-kind row (IncomeCategoryError / InactiveCategoryError / CategoryNotFoundError — the latter reused from the categories service so the action layer maps one i18n key across both surfaces); the parent month is resolved via `findMonthById` BEFORE category validation so a missing month surfaces as `MonthNotFoundError` first (PRD §5.1). `observations` is OPTIONAL — the schema column is nullable and the service normalises empty/whitespace strings to `null` (PRD §6.7). Money round-trips through `parseAmount` → integer cents → `formatCents` per ARCH §8 / ADR-5; negative amounts allowed (PRD UC-16). Hard delete on every remove (PRD C15); the `null` return from the repo (row not found / wrong tenant / concurrent delete) maps to `ActualNotFoundError`. The service NEVER mutates `month_fixed_line.remaining_amount` — verified by `tests/integration/actuals.test.ts` #11 (snapshot of the reserved-line amounts is unchanged around two `addActual` calls). Server actions `addActualAction` / `editActualAction` / `deleteActualAction` in `src/actions/actuals.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath` pattern (ARCH §5 rule 3) and translate domain errors into `ActualActionError = "incomeCategory" | "inactiveCategory" | "categoryNotFound" | "actualNotFound" | "monthNotFound" | "amountFormat" | "validation"`. The `observations` field is `z.union([z.string().max(500), z.null()]).optional()` and the client uses a small `readObservations(formData)` helper to coerce `FormDataEntryValue` to a trimmed `string | null`. The UI replaces the UC-06 "coming next" placeholder with a real `ActualsScreen` client (`src/app/[locale]/months/[year]/[month]/actuals-screen.tsx`) — receives `initialActuals` (serialized with `amountCents` integer cents, ADR-5 / ARCH §8) + ACTIVE expense categories for the picker + the full category map for historical lookups (so a row whose category was later deactivated still shows its name + an inactive-note caption). The add form carries a free-text `name`, an OPTIONAL `observations` input, the AmountInput, and the category select. Each row toggles into an inline edit mode (the AmountInput is seeded via `centsToInputString` and observations rehydrate to an empty string when `null` was stored). PRD §15 #11 + #14 + #15 covered by `tests/integration/actuals.test.ts` (16 cases) and `tests/e2e/actuals.spec.ts` (chromium + mobile-safari, 8 cases).
- Month incomes (UC-07): add / edit / hard-delete of month-scoped income rows (PRD UC-09 / §6.5 / C15 / §7.8). New `userId`-first repository `src/server/repositories/income.ts` (`insertIncome` / `findIncomeById` / `updateIncome` / `deleteIncome` / `listIncomesForMonth`); `month_income` has no direct `user_id` column so every read/write joins on `month` with `month.user_id = userId` (PRD §5.1). `update` uses PG `UPDATE ... FROM month WHERE month.user_id = userId` in one statement; `delete` uses a raw `sql` template (Drizzle's PG delete builder doesn't expose `.using()`) so the WHERE clause enforces tenancy in a single statement — PRD C15 / §13 hard delete on month-scoped money rows. New service `src/server/services/incomes.ts` owns `addIncome` / `editIncome` / `deleteIncome` / `listIncomesForMonth`. Domain rules: the target category must be an ACTIVE income-kind row (ExpenseCategoryError / InactiveCategoryError / CategoryNotFoundError — the latter is reused from the categories service so the action layer maps one i18n key across both surfaces); the parent month is resolved via `findMonthById` so a missing month surfaces as `MonthNotFoundError` BEFORE the user picks a category (PRD §5.1). Money round-trips through `parseAmount` → integer cents → `formatCents` per ARCH §8 / ADR-5; negative amounts allowed (PRD UC-16). Hard delete on every remove (PRD C15). Server actions `addIncomeAction` / `editIncomeAction` / `deleteIncomeAction` in `src/actions/incomes.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath` pattern (ARCH §5 rule 3) and translate domain errors into `IncomeActionError = "expenseCategory" | "inactiveCategory" | "categoryNotFound" | "incomeNotFound" | "monthNotFound" | "amountFormat" | "validation"`. The actions carry the active `(year, month)` from the caller's URL so revalidation targets the exact workspace route. The UI replaces the UC-06 "coming next" placeholder with a real `IncomesScreen` client (`src/app/[locale]/months/[year]/[month]/incomes-screen.tsx`) — receives `initialIncomes` (serialized with `amountCents` integer cents, ADR-5 / ARCH §8) + ACTIVE income categories for the picker + the full category map for historical lookups (so a row whose category was later deactivated still shows its name + an inactive-note caption). The row toggles into an inline edit mode (`centsToInputString` seeds the AmountInput) and offers Delete with a `window.confirm` prompt. New helper `centsToInputString(cents)` in `src/i18n/format.ts` is the inverse of `formatMoney` for the integer-cents part so the edit form rehydrates the wire string from the stored cents. PRD §15 #5 covered by `tests/integration/incomes.test.ts` (11 cases — happy create, negative amount, expense category rejected, inactive category rejected, unknown category rejected, edit replaces all fields, hard delete + row gone, edit/delete on missing row, historical income keeps rendering after category deactivate, two-tenant isolation including cross-tenant edit/delete, income CRUD never touches the template table) and `tests/e2e/incomes.spec.ts` (chromium + mobile-safari, 6 cases — add → edit → delete happy path with DB row-count check, no-active-categories empty state, Spanish variant with `Añadir`).
- Month creation, cloning & home navigation (UC-06): manual month creation with the one-time template clone (PRD UC-08 / UC-14 / UC-19 / C6 / C12 / §7.8). New `userId`-first repository `src/server/repositories/month.ts` (`findMonthByPeriod` / `insertMonth` / `listMonths` / `insertClonedLines` / `listMonthFixedLines` / `listMonthIncomes` / `listMonthActuals`); every function filters by `userId` per ARCH §5 rule 1. New service `src/server/services/months.ts` owns `createMonth(userId, year, month)` — pre-flight duplicate check via `findMonthByPeriod`, then `db.transaction(async tx => ...)` opens ONE tx, inserts the `month` row, fetches the user's ACTIVE templates via `listActiveTemplates(userId, tx)`, and bulk-inserts one `month_fixed_line` row per template with `remaining_amount = original_amount = template amount` and `origin = 'cloned'` (PRD C17, §7.8). `month_user_period_uk` backstops duplicate creation (PRD UC-08) and surfaces as `DuplicateMonthError`. Incomes are NEVER cloned (PRD §7.8); months never sync with templates or other months after creation — verified by an integration test that snapshots month money-row counts around a full template-edit + deactivate cycle. Server action `createMonthAction` in `src/actions/months.ts` follows the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]', 'page')` pattern (ARCH §5 rule 3) and translates `DuplicateMonthError` into `{ ok: false, error: "duplicate" }`. Cookie helpers (`readLastOpenedMonthCookie` / `setLastOpenedMonthCookie` + the `LAST_OPENED_MONTH_COOKIE` constant + `LastOpenedMonth` interface) live in `src/server/cookies/last-opened-month.ts` (NOT inside `actions/months.ts`) because Next 16 forbids non-async exports from `"use server"` files. The home page (`src/app/[locale]/page.tsx`) reads the cookie, re-validates against the DB, and redirects to the matching workspace if it exists (PRD UC-14); otherwise renders the empty-state create form (with PRD §19 "Nothing is created automatically" copy) or the month list (newest first) with a collapsible create form. The workspace (`src/app/[locale]/months/[year]/[month]/page.tsx`) is a read-only skeleton that renders the cloned reserved lines grouped by kind (committed first, then estimated) with `formatMoney(parseAmount(remaining_amount), currency)` labels; Incomes + Actuals render "coming next" placeholders for UC-07/08. Out-of-range year/month route segments surface as 404 (range validation lives in the page's RSC guard). Direct visits to the workspace touch the resume cookie via a tiny `MonthTouchClient` (`document.cookie` after mount) because Next 16 also forbids `cookies().set()` from RSC bodies; the cookie is a UX hint, not a security boundary (PRD §5.4). PRD §15 #3 + #4 + #18 covered by `tests/integration/months.test.ts` (10 cases — empty create, duplicate rejection, full clone snapshot for committed + estimated, inactive templates excluded from clone, no-cross-month sync, no-cross-tenant leakage, atomicity on duplicate, sorted list, month-not-found) and `tests/e2e/months.spec.ts` (chromium + mobile-safari, 6 cases — empty-state → form create → cloned lines → cookie resume → second month newest-first list view → duplicate inline error → Spanish variant).

### Changed
- Split `src/components/amount-input.ts` (helpers only) into `src/components/amount-input-helpers.ts` because the two-file split (`.tsx` + `.ts`) confuses the path resolver; `AmountInput` now lives unambiguously at `src/components/amount-input.tsx`. `AmountInput` also accepts an `id` prop so callers can link `<label htmlFor>` and target the underlying `<input>` from tests.

### Added (previous slices)
- Profile settings screen (UC-04): display-only currency label (PRD C9, UC-15, §7.6). New `userId`-first mutator `updateCurrency(userId, currency, tx)` in `src/server/repositories/profile-settings.ts` writes ONLY `profile_settings.currency` + bumps `updated_at` — the `char(3)` column type is the DB-layer defense in depth behind the Zod `^[A-Z]{3}$` regex in `currencySchema`. New service `src/server/services/settings.ts` owns the domain rule (ISO 4217 alpha-3, otherwise `InvalidCurrencyError`) and verifies the tenant actually has an `app_user` row before the update — so a missing profile row surfaces as `ProfileSettingsNotFoundError` rather than a silent no-op. New server action `updateCurrencyAction` in `src/actions/settings.ts` follows the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/settings', 'page')` pattern (ARCH §5 rule 3) and revalidates BOTH the settings page AND `/[locale]` so the home surfaces the new label on next render. New display helper `formatMoney(cents, currency)` in `src/i18n/format.ts` returns `<sign><amount> <LABEL>` with exactly two fractional digits and no grouping separator (PRD C9: dot-decimal in BOTH locales, so display mirrors input); throws on non-integer cents or non-3-letter labels (ADR-5, ARCH §8). New settings screen at `/[locale]/settings` (screen 8) is an RSC shell that loads the row, renders a `formatMoney` preview block (positive / zero / negative sample amounts in the active currency so the user can see the change before saving), and hands off to a client `SettingsForm` that uses React 19 `useActionState` + a native `<select>` for the picker — successful save bumps `formKey` so the select's `defaultValue` re-reads from the freshly-revalidated `currentCurrency` prop, and the confirmation surfaces through an `aria-live="polite"` region. i18n bundles extended with `settings.currentLabel`, `settings.preview.*`, `settings.actions.save`, `settings.status.saved`; existing `validation.currencyInvalid` is reused for the keyed error. PRD §15 #15 + the 2-decimal formatting invariant for every amount-rendering slice covered by `tests/unit/format-money.test.ts` (8 cases including the full `numeric(14,2)` range and rejection of non-integer cents) and `tests/integration/settings.test.ts` (7 cases — EUR default, single-row update, no-other-money-row-touched, tenant isolation, unknown-user `ProfileSettingsNotFoundError`, invalid codes rejected, repeated updates keep the row count at 1). Playwright e2e `tests/e2e/settings.spec.ts` (chromium + mobile-safari) reuses the auth-bypass helper from UC-03 and asserts the EUR → USD label swap in both the preview block and the live DB row, plus the Spanish variant.
- Fixed/estimated templates catalog (UC-05): per-user catalog of `committed` and `estimated` templates that are CLONED into a new month exactly once at month creation (PRD C17, §6.3, §7.8). New `userId`-first repository `src/server/repositories/template.ts` (`listTemplates` / `listTemplatesByKind` / `listActiveTemplates` / `listActiveTemplatesByKind` / `findTemplateById` / `insertTemplate` / `updateTemplate` / `deactivateTemplate` / `reactivateTemplate`); the `listActive*` family is the clone-source query for UC-06 and EXCLUDES soft-deleted rows, while `listTemplatesForManagement` includes them so the user can scan history. New service `src/server/services/templates.ts` validates that the target category is an ACTIVE expense category (using `findCategoryById` from the category repo and reusing `CategoryNotFoundError`) and round-trips the amount string through `parseAmount` → integer cents → `formatCents` per ARCH §8 / ADR-5. Soft delete is `active=false, deleted_at=now()`; a double-deactivate is mapped to `TemplateAlreadyInactiveError` via the repo's `WHERE active = true` guard, and reactivate reverses it (also `TemplateAlreadyActiveError` on double-reactivate). Editing a template never rewrites month rows — verified by `tests/integration/templates.test.ts` #10 (snapshot of month money-row counts is unchanged around a full CRUD cycle). Server actions in `src/actions/templates.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/templates', 'page')` pattern (ARCH §5 rule 3) and translate domain errors into `TemplateActionError = "validation" | "incomeCategory" | "inactiveCategory" | "notFound" | "alreadyInactive" | "alreadyActive" | "amountFormat"`. The UI is a single screen at `/[locale]/templates` (screen 7) with a committed/estimated tablist, an `AddTemplateForm` that uses `useActionState` + the `AmountInput` component to enforce the wire format, and a `TemplateRow` that toggles into an inline edit mode and offers deactivate/reactivate. Inactive rows render with reduced opacity + an "Inactive" badge. The home page now links to `/<locale>/templates` (alongside categories and settings) so the screen is reachable from the app. The page also surfaces the PRD §19 clone copy ("Fixed and estimated lines are copied when the month is created. This month is independent after that.") as a muted banner — also added to `months.clone` for UC-06. PRD §15 #11 + #16 + #17 covered by `tests/integration/templates.test.ts` (13 cases — happy create, negative amount accepted, income category rejected, inactive category rejected, unknown category is `CategoryNotFoundError`, edit preserves month rows, soft delete hides from clone query but keeps management row, double-deactivate, reactivate restores clone visibility, double-reactivate, kind bucket isolation, two-tenant isolation, no physical DELETE on `template`); Playwright e2e `tests/e2e/templates.spec.ts` (chromium + mobile-safari) reuses the auth-bypass helper from UC-03 and walks the create → deactivate → reactivate happy path plus the Spanish variant.
- Categories management (UC-03): per-user global catalogs of expense and income categories with soft-delete-only (PRD §13). New `userId`-first repository `src/server/repositories/category.ts` (`listCategories`, `listActiveCategories`, `findCategoryById`, `insertCategory`, `updateCategoryName`, `deactivateCategory`, `reactivateCategory` — every function filters by `user_id` per ARCH §5 rule 1; the deactivate/reactivate guards require `active = true/false` respectively so a double-deactivate is a no-op and surfaces as `CategoryAlreadyInactiveError`). New service `src/server/services/categories.ts` with named domain errors (`DuplicateCategoryNameError`, `CategoryNotFoundError`, `CategoryAlreadyInactiveError`, `CategoryAlreadyActiveError`); unique-name collision is caught by walking the Drizzle `cause` chain to Postgres SQLSTATE 23505 and matching `constraint_name === 'category_active_name_uk'` — so a future schema addition can't accidentally be mis-translated. New server actions `src/actions/categories.ts` (`createCategoryAction` / `renameCategoryAction` / `deactivateCategoryAction` / `reactivateCategoryAction`) follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/categories', 'page')` pattern (ARCH §5 rule 3) and read `x-next-intl-locale` from headers so unauthenticated callers land on a locale-prefixed sign-in URL. New management screen at `/[locale]/categories` (screens 5–6) with an expense/income tablist, an `AddCategoryForm` using React 19 `useActionState` to surface keyed validation errors, and a `CategoryRow` that toggles into an inline rename mode and offers deactivate / reactivate. Inactive rows render with reduced opacity and an "Inactive" badge so users can scan the catalog and re-enable old entries without confusing them with active ones (PRD §6.2 — history stays intact via the FK join). i18n bundles extended with `categories.tabs.*`, `categories.empty.*`, `categories.actions.*`, `categories.help`, `validation.duplicateCategoryName`, `validation.categoryNotFound`. PRD §15 #5 covered by `tests/integration/categories.test.ts` (12 cases — happy create, duplicate name, soft-delete + re-create with same name, deactivate, double-deactivate, reactivate, listActive excludes inactive, listAll includes inactive, two-tenant isolation) and `tests/e2e/categories.spec.ts` (chromium + mobile-safari, 6 cases — happy create, duplicate inline error, deactivate/reactivate, Spanish variant).
- i18n shell (UC-02): next-intl with the `[locale]` App Router segment, locales `en` (default) and `es`, `localePrefix: 'always'`, and locale resolution order `NEXT_LOCALE` cookie → `Accept-Language` → `en` (PRD C4). New `src/i18n/routing.ts` is the single source of truth for supported locales; `src/i18n/request.ts` resolves the request locale and lazy-loads the matching bundle; `src/i18n/navigation.ts` exposes locale-aware `<Link>` / `redirect` / `usePathname` / `useRouter`. Edge middleware in `src/middleware.ts` (matches everything except `/api/*`, `/_next/*`, `/_vercel/*`, and dotfiles) rewrites and persists the cookie; `pages.error = '/403'` keeps working because the middleware redirects to `/<locale>/403` for the user's active locale. `next.config.ts` wraps the build with `createNextIntlPlugin('./src/i18n/request.ts')` so the standalone output-tracer keeps the request config (otherwise runtime fails with "Couldn't find next-intl config file"). New client primitives: `src/components/language-switcher.tsx` (uses `<Link locale=…>` — the middleware persists the cookie on navigation) and `src/components/amount-input.tsx` + `src/components/amount-input.ts` (controlled input enforcing `^-?\d{1,12}\.\d{2}$` in both locales, with the classifier extracted so it's unit-testable; rejects `1234,56` regardless of locale per PRD C9). Locale-aware helpers in `src/i18n/format.ts` (`monthName`, `monthYear`, `shortDate`, `amountToParts`) so month names follow `Intl.DateTimeFormat` and are never hardcoded (PRD §11). Full `en.json` / `es.json` seeded with the reserved namespaces called out in the slice (`language`, `common`, `nav`, `validation`, `warnings`, `months`, `categories`, `templates`, `settings`, `pwa`) plus a parity test that fails the build when a key drifts between locales. Root layout now reads the `x-next-intl-locale` header (set by the middleware) so `<html lang>` reflects the active locale instead of always being `en`. Vitest suites cover the new helpers (`tests/unit/month-format.test.ts` — `monthName` for both locales across all 12 months).

### Fixed
- Signed-in users landing on `/[locale]` were silently redirected back to `/[locale]/sign-in`, making the Google sign-in flow look broken. The home now renders a minimal signed-in confirmation (email + display name + sign-out) until UC-06 delivers the real month workspace.
- Auth.js `jwt` callback was reading the Google `sub` from `user.id`, which without an Auth.js DB adapter is a freshly-minted `crypto.randomUUID()` generated by `@auth/core`'s `handleLoginOrRegister` (see `node_modules/.pnpm/@auth+core@0.41.3/.../actions/callback/index.js:70-80`). The callback now reads the stable OAuth subject from `account.providerAccountId` (falling back to `profile.sub`), short-circuits when `token.userId` is already set, and the `session` callback writes the internal `app_user.id` onto `session.user.id` — so the home page (and every later repository call) sees the right tenant id.

### Added (UC-00 / UC-01)
- Google sign-in, allowlist & tenancy (UC-01): Auth.js v5 with the Google provider and an `ALLOWED_EMAILS` allowlist enforced in the `signIn` callback (trim + lowercase normalization), JWT session strategy carrying the internal `app_user.id` (resolved from `google_sub` in the `jwt` callback, exposed via the `next-auth` module augmentation in `src/types/next-auth.d.ts`). First-time sign-in provisions `app_user` + `profile_settings(currency='EUR')` atomically via the `upsertUserOnSignIn` service; denied users get no session and no DB row and land on `/[locale]/403` through `pages.error = '/403'`. New `requireUserId()` primitive in `src/server/auth/require-user-id.ts` (cached with `React.cache`) reads the session in the data-access layer and redirects to the locale-prefixed sign-in when absent, so every repository keeps `userId` first per PRD §5.1. `userId`-first repositories `src/server/repositories/user.ts` and `src/server/repositories/profile-settings.ts`. Locale-aware sign-in and 403 screens at `/[locale]/sign-in` and `/[locale]/403` render `auth.signIn.*` / `auth.forbidden.*` copy from `src/i18n/messages/{en,es}.json` (minimal `loadMessages` helper — UC-02 swaps this for the full next-intl routing setup). `server-only` dependency + Vitest stub so the marker package resolves in Node test runs. PRD scenarios #1 (allowlist hit/miss → 403) and #2 (two users isolated) covered by `tests/unit/allowlist.test.ts` and `tests/integration/auth.test.ts`.
- Product requirements document `docs/prds/GLOBAL.md` — MVP money rules, constraints C1–C18, use cases UC-01…UC-19, and normative test scenarios.
- Architecture document `docs/architecture/ARCHITECTURE.md` — ADR-1…ADR-10 (Next.js BFF, Auth.js v5 with Google + allowlist, Drizzle ORM, Serwist PWA, next-intl, Vitest + Playwright).
- Database schema proposal `docs/database/database.dbml` — 8 tables with singular names, 3 enums, PostgreSQL 16, dbdiagram.io DBML.
- Use-case breakdown `docs/usecases/` — 13 implementation slices (UC-00…UC-12) plus `UC-INDEX.md` with build order, dependencies, and PRD test-scenario mapping.
- Agent-facing documentation guide `docs/README.md` and live implementation tracker `docs/IMPLEMENTATION-STATUS.md`.
- Agent conventions in `AGENTS.md`, including mandatory implementation-status tracking and changelog rules.
- Foundations & database bootstrap (UC-00): Drizzle ORM + `postgres-js` client, full schema (`src/server/db/schema.ts`) materialising the 8 tables and 3 enums of `database.dbml`, first migration with the partial unique index on `category(name) WHERE active` and the `month_range_ck` check constraint, integer-cents money helpers (`src/server/money.ts`), shared Zod amount schema (`src/server/validators.ts`), Vitest unit + integration suites, Playwright config, multi-stage `Dockerfile` (node:22-alpine, Next.js `standalone` output, runtime migration entrypoint) and `docker-compose.yml` wiring `app` + `postgres:16-alpine` on the `expenses-net` bridge network with a healthcheck-gated `depends_on`.
- UC-00 follow-up: completed the remaining ARCH §10 scaffolding steps — `app/` moved to `src/app/`, `shadcn init` (base-nova preset, neutral palette, lucide icons, `src/components` + `src/lib`), `next-auth@beta` and `next-intl` dependencies installed, Serwist wired (`src/app/sw.ts` + `src/app/manifest.ts` with precache-only app shell, no runtime data caching per PRD C11). Build uses `next build --webpack` (and dev uses `next dev --webpack`) because `@serwist/next` is webpack-based; Next 16 Turbopack is opt-in for the rest of the toolchain.
