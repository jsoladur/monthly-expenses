# Implementation status — Monthly Expenses

> **Live tracker.** The coding agent updates this file at the end of every work session on a use case (see `AGENTS.md` §4).
> **Status values:** `PENDING` → `IN_PROGRESS` → `DONE`. Exactly one row per use-case file in `usecases/`.

## Rules for updating (agents)

- Set `IN_PROGRESS` when you start a slice. Only ONE slice may be `IN_PROGRESS` at a time.
- Set `DONE` only when the definition of done passes: typecheck + lint clean · the slice's mapped PRD §15 test scenarios are green · no `userId`-less repository call exists (ARCH §10).
- Fill **Completed** with an ISO date (`YYYY-MM-DD`) and **Notes** with anything the next agent must know (deviations, follow-ups, caveats). Leave empty otherwise.
- A `CHANGELOG.md` entry under `[Unreleased]` is MANDATORY in the same commit as the `DONE` flip.
- Never edit a `DONE` row except to fix a factual error. Regressions are handled as new work and a `Fixed` entry in the changelog.
- A slice may only be started when every slice in its "Depends on" column (`usecases/UC-INDEX.md`) is `DONE`.

## Progress

- Slices DONE: **16 / 16**
- Next up: **—** (all use-case slices shipped)

## Status table

| Slice | Title | Status | Completed | Notes |
| --- | --- | --- | --- | --- |
| UC-00 | Foundations & database bootstrap | DONE | 2026-08-23 | Drizzle schema, first migration, money helpers, Zod amount schema, Vitest (unit + integration), Playwright config, multi-stage Dockerfile + docker-compose (app + postgres on `expenses-net`), full ARCH §10 scaffolding (src/app layout, shadcn init with base-nova, next-auth@beta, next-intl, Serwist `manifest.ts` + `sw.ts`). Build runs with `--webpack` so the Serwist plugin works (Next 16 Turbopack is opt-in). |
| UC-01 | Google sign-in, allowlist & tenancy | DONE | 2026-08-23 | Auth.js v5 with Google provider + `ALLOWED_EMAILS` allowlist (`signIn` callback normalizes trim + lowercase), JWT session carrying the internal `app_user.id` (`jwt` resolves it by `google_sub`, `session` exposes it via the `next-auth` module augmentation in `src/types/next-auth.d.ts`). New `app_user` + `profile_settings(currency='EUR')` created in one transaction via the `upsertUserOnSignIn` service. `requireUserId()` lives in the data-access layer (`src/server/auth/require-user-id.ts`), wrapped in `React.cache`. Sign-in (`/[locale]/sign-in`) and 403 (`/[locale]/403`) pages render `auth.signIn.*` / `auth.forbidden.*` copy from `src/i18n/messages/{en,es}.json`; UC-02 will replace the minimal `loadMessages` helper with the full next-intl `defineRouting` + middleware setup. Denied users land on `/[locale]/403` via `pages.error = '/403'`. PRD scenarios #1 (allowlist hit/miss) + #2 (two users isolated) covered by `tests/unit/allowlist.test.ts` and `tests/integration/auth.test.ts`. Added `server-only` dep + Vitest alias stub so RSC markers resolve cleanly in Node test runs. |
| UC-02 | i18n shell (en/es) | DONE | 2026-08-23 | next-intl with `[locale]` segment, locales `en`/`es`, `localePrefix: 'always'`, resolution order `NEXT_LOCALE` cookie → `Accept-Language` → `en`. Routing config in `src/i18n/routing.ts` (single source of truth) + `src/i18n/request.ts` (locale + bundle loader) + `src/i18n/navigation.ts` (typed `Link` / `redirect` / `usePathname` / `useRouter`). Edge middleware in `src/middleware.ts` (matcher excludes `/api`, `/_next`, `/_vercel`, dotfiles so Auth.js callbacks stay intact — ARCH §3.2 rule 4 keeps `requireUserId()` as the canonical tenancy check, NOT the middleware). `pages.error = '/403'` keeps working because the middleware redirects to `/<locale>/403` for the user's active locale. `next.config.ts` wraps the build with `createNextIntlPlugin('./src/i18n/request.ts')` so the standalone output-tracer keeps the request config (without it, runtime fails with "Couldn't find next-intl config file" because nothing statically imports the file in the dependency graph). New client primitives: `src/components/language-switcher.tsx` (uses `<Link locale=…>` so the middleware persists the cookie on navigation) and `src/components/amount-input.tsx` + the extracted `classifyAmount` helper in `src/components/amount-input.ts` (controlled input enforcing `^-?\d{1,12}\.\d{2}$` in both locales; rejects `1234,56` regardless of locale per PRD C9 / §11). Locale-aware helpers in `src/i18n/format.ts` (`monthName`, `monthYear`, `shortDate`, `amountToParts`) so month names follow `Intl.DateTimeFormat` and are never hardcoded (PRD §11). Full `en.json` / `es.json` seeded with the reserved namespaces called out in the slice (`language`, `common`, `nav`, `validation`, `warnings`, `months`, `categories`, `templates`, `settings`, `pwa`) plus a parity test that fails the build when a key drifts between locales. Root layout now reads `x-next-intl-locale` from the middleware so `<html lang>` reflects the active locale. UC-02 acceptance #1–4 (browser `es` → Spanish shell, browser `fr` → English fallback, language switcher persistence across reloads, translated 403) covered by `tests/e2e/i18n.spec.ts` on Chromium + mobile-safari. Helper/format/AmountInput coverage in `tests/unit/i18n-format.test.ts`, `tests/unit/amount-input.test.ts`, `tests/unit/i18n-messages.test.ts`. |
| UC-03 | Categories (expense & income) | DONE | 2026-08-23 | Per-user global catalogs of expense & income categories with soft delete only (PRD §13, PRD §6.2). New `userId`-first repository `src/server/repositories/category.ts` (listCategories / listActiveCategories / findCategoryById / insertCategory / updateCategoryName / deactivateCategory / reactivateCategory — every function filters by `user_id` per ARCH §5 rule 1). Service `src/server/services/categories.ts` exposes the same operations plus named domain errors (`DuplicateCategoryNameError`, `CategoryNotFoundError`, `CategoryAlreadyInactiveError`, `CategoryAlreadyActiveError`). Unique-name collision is caught at the service layer by walking the Drizzle `cause` chain to Postgres 23505 and checking `constraint_name === 'category_active_name_uk'` — so a future schema addition can't accidentally surface as "duplicate category name". Soft delete is `active=false, deleted_at=now()`; the repository guards `WHERE active = true` so a double-deactivate doesn't overwrite the original `deleted_at` and is mapped to `CategoryAlreadyInactiveError`. Reactivate re-uses the partial unique index so a name collision at reactivate time throws `DuplicateCategoryNameError`. Server actions in `src/actions/categories.ts` (createCategoryAction / renameCategoryAction / deactivateCategoryAction / reactivateCategoryAction) follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/categories', 'page')` pattern (ARCH §5 rule 3) and read `x-next-intl-locale` from headers so unauthenticated callers land on the locale-prefixed sign-in URL. The UI is one screen at `/[locale]/categories` (screens 5–6) with an expense/income tablist, an `AddCategoryForm` using `useActionState` to surface keyed validation errors, and a `CategoryRow` that toggles into an inline rename mode and offers deactivate / reactivate. Inactive rows render with reduced opacity + a "Inactive" badge so users can scan the catalog and re-enable old entries without confusing them with active ones (PRD §6.2 — history stays intact via the FK join). i18n bundles extended with `categories.tabs.*`, `categories.empty.*`, `categories.actions.*`, `categories.help`, `validation.duplicateCategoryName`, `validation.categoryNotFound`. PRD §15 #16 + #11-picker-prep covered by `tests/integration/categories.test.ts` (12 cases — happy path, duplicate rejection, soft-delete frees the name, deactivate semantics, reactivate semantics, cross-tenant not-found leak, two-tenant isolation, no physical DELETE). Playwright e2e `tests/e2e/categories.spec.ts` (chromium + mobile-safari) bypasses the Google OAuth dance by seeding an `app_user` row + forging an Auth.js session cookie via `next-auth/jwt#encode` (helper in `tests/e2e/_helpers/auth.ts`); the spec covers create / duplicate-rejected / deactivate / reactivate and the Spanish-language variant. |
| UC-04 | Profile settings (currency) | DONE | 2026-08-23 | Display-only currency label (PRD C9, UC-15, §7.6). The `profile_settings` row is provisioned by UC-01 at first sign-in (default `EUR`); this slice adds the read path used app-wide to label amounts and the mutation that updates the column. New `userId`-first repository mutator `updateCurrency(userId, currency, tx)` in `src/server/repositories/profile-settings.ts` writes ONLY `profile_settings.currency` + bumps `updated_at` — the `char(3)` column type is the DB-layer defense in depth behind the Zod regex `^[A-Z]{3}$` from `currencySchema`. New service `src/server/services/settings.ts` owns the domain rule (currency is an ISO 4217 alpha-3 code, otherwise `InvalidCurrencyError`) and verifies the tenant actually has an `app_user` row before the update — so a missing profile row surfaces as `ProfileSettingsNotFoundError` rather than a silent no-op. New server action `updateCurrencyAction` in `src/actions/settings.ts` follows the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/settings', 'page')` pattern (ARCH §5 rule 3) and revalidates BOTH the settings page AND `/[locale]` so the home surfaces the new label on next render. New display helper `formatMoney(cents, currency)` in `src/i18n/format.ts` returns `<sign><amount> <LABEL>` with exactly two fractional digits and no grouping separator (PRD C9: dot-decimal in BOTH locales, so display mirrors input); throws on non-integer cents or non-3-letter labels (ADR-5, ARCH §8). New settings screen at `/[locale]/settings` (screen 8) is an RSC shell that loads the row, renders a `formatMoney` preview block (positive / zero / negative sample amounts in the active currency so the user can see the change before saving), and hands off to a client `SettingsForm` that uses React 19 `useActionState` + a native `<select>` for the picker — successful save bumps `formKey` so the select's `defaultValue` re-reads from the freshly-revalidated `currentCurrency` prop, and the confirmation surfaces through an `aria-live="polite"` region. i18n bundles extended with `settings.currentLabel`, `settings.preview.*`, `settings.actions.save`, `settings.status.saved`; existing `validation.currencyInvalid` is reused for the keyed error. PRD §15 #15 (currency label + 2 decimals, no FX) + the 2-decimal formatting invariant for every amount-rendering slice are covered by `tests/unit/format-money.test.ts` (8 cases including the full `numeric(14,2)` range and rejection of non-integer cents) and `tests/integration/settings.test.ts` (7 cases — EUR default, single-row update, no-other-money-row-touched, tenant isolation, unknown-user `ProfileSettingsNotFoundError`, invalid codes rejected, repeated updates keep the row count at 1). Playwright e2e `tests/e2e/settings.spec.ts` (chromium + mobile-safari) reuses the auth-bypass helper from UC-03 and asserts the EUR → USD label swap in both the preview block and the live DB row, plus the Spanish variant. |
| UC-05 | Fixed/estimated templates | DONE | 2026-08-23 | Per-user catalog of fixed (`committed`) and estimated templates that are CLONED into a new month exactly once at month creation (PRD C17, §6.3, §7.8). Templates are expense-only: a template targeting an INCOME category throws `IncomeCategoryError` and a template targeting an inactive/soft-deleted category throws `InactiveCategoryError` — both surfaced as keyed i18n errors in the action. `userId`-first repository `src/server/repositories/template.ts` (`listTemplates` / `listTemplatesByKind` / `listActiveTemplates` / `listActiveTemplatesByKind` / `findTemplateById` / `insertTemplate` / `updateTemplate` / `deactivateTemplate` / `reactivateTemplate`); the `listActive*` family is the clone-source query for UC-06 and EXCLUDES soft-deleted rows, while `listTemplatesForManagement` includes them so the user can scan history (PRD §6.3). The service `src/server/services/templates.ts` validates that the target category is an ACTIVE expense category (using `findCategoryById` from the category repo and reusing `CategoryNotFoundError`) and round-trips the amount string through `parseAmount` → integer cents → `formatCents` per ARCH §8 / ADR-5. Soft delete is `active=false, deleted_at=now()`; a double-deactivate is mapped to `TemplateAlreadyInactiveError` via the repo's `WHERE active = true` guard, and reactivate reverses it (also `TemplateAlreadyActiveError` on double-reactivate). Editing a template never rewrites month rows — verified by `tests/integration/templates.test.ts` (#10: snapshot of `month` + `month_income` + `month_fixed_line` + `month_actual_expense` row counts is unchanged around a full CRUD cycle). Server actions in `src/actions/templates.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/templates', 'page')` pattern (ARCH §5 rule 3) and translate domain errors into `TemplateActionError = "validation" | "incomeCategory" | "inactiveCategory" | "notFound" | "alreadyInactive" | "alreadyActive" | "amountFormat"`. The UI is a single screen at `/[locale]/templates` (screen 7) with a committed/estimated tablist, an `AddTemplateForm` that uses `useActionState` + the `AmountInput` component to enforce the wire format, and a `TemplateRow` that toggles into an inline edit mode and offers deactivate/reactivate. Inactive rows render with reduced opacity + an "Inactive" badge. The home page now links to `/<locale>/templates` (alongside categories and settings) so the screen is reachable from the app. The page also surfaces the PRD §19 clone copy ("Fixed and estimated lines are copied when the month is created. This month is independent after that.") as a muted banner — also added to `months.clone` for UC-06. PRD §15 #11 (expense templates) + #16 (no cross-tenant leakage) + #17 (tenants isolated) covered by `tests/integration/templates.test.ts` (13 cases — happy create, negative amount accepted, income category rejected, inactive category rejected, unknown category is `CategoryNotFoundError`, edit preserves month rows, soft delete hides from clone query but keeps management row, double-deactivate, reactivate restores clone visibility, double-reactivate, kind bucket isolation, two-tenant isolation, no physical DELETE on `template`); Playwright e2e `tests/e2e/templates.spec.ts` (chromium + mobile-safari) reuses the auth-bypass helper from UC-03 and walks the create → deactivate → reactivate happy path plus the Spanish variant. The `AmountInput` component now accepts an `id` prop so tests can target the underlying `<input>` directly (the earlier `useId`-only id collided with the `htmlFor` label binding). Renamed `src/components/amount-input.ts` → `amount-input-helpers.ts` because the two-file split confuses the path resolver — `AmountInput` now lives unambiguously at `src/components/amount-input.tsx`. |
| UC-06 | Month creation, cloning & home | DONE | 2026-08-23 | Manual month creation with the one-time template clone + the resume-cookie home/list navigation (PRD UC-08 / UC-14 / UC-19 / C6 / C12 / §7.8). New `userId`-first repository `src/server/repositories/month.ts` (`findMonthByPeriod` / `insertMonth` / `listMonths` / `insertClonedLines` / `listMonthFixedLines` / `listMonthIncomes` / `listMonthActuals`); every function filters by `userId` per ARCH §5 rule 1, and the `Tx` plumbing is reused from the other repos so the service can compose insert + clone in one transaction. New service `src/server/services/months.ts` owns the `createMonth(userId, year, month)` flow — pre-flight duplicate check via `findMonthByPeriod`, then `db.transaction(async tx => ...)` opens one tx, inserts the `month` row, fetches the user's ACTIVE templates via `listActiveTemplates(userId, tx)`, and bulk-inserts one `month_fixed_line` row per template with `remaining_amount = original_amount = template amount` and `origin = 'cloned'` (PRD C17, §7.8). `month_user_period_uk` backstops the duplicate check (PRD UC-08) and surfaces as `DuplicateMonthError` if a parallel request slips through; `MonthNotFoundError` is thrown by `getMonthWorkspace`. `getMonthList` returns months newest-first (PRD UC-14); `getMonthWorkspace` runs the three month-money-row reads in parallel since they're independent. Incomes are NEVER cloned (PRD §7.8); months never sync with templates or other months after creation (verified by an integration test that snapshots `month` + `month_fixed_line` row counts around a full template-edit + deactivate cycle). Server action `createMonthAction` in `src/actions/months.ts` follows the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]', 'page')` pattern (ARCH §5 rule 3) and translates `DuplicateMonthError` into `{ ok: false, error: "duplicate" }`. On success it sets the `last_opened_month` cookie (PRD UC-14, §5.4) so re-opening the home redirects straight into the just-created month. Cookie helpers (`readLastOpenedMonthCookie` / `setLastOpenedMonthCookie` + the `LAST_OPENED_MONTH_COOKIE` constant) live in `src/server/cookies/last-opened-month.ts` (NOT inside `actions/months.ts`) because Next 16 forbids non-async exports from `"use server"` files; the helpers use `cookies()` directly and are called from RSC + server actions. The home page (`src/app/[locale]/page.tsx`) reads the cookie first, re-validates against the DB, and redirects to the matching workspace if it exists; otherwise renders the empty-state create form (with the PRD §19 "Nothing is created automatically" copy) or the month list (newest first) with a collapsible create form. The workspace (`src/app/[locale]/months/[year]/[month]/page.tsx`) is a read-only skeleton that renders the cloned reserved lines grouped by kind (committed first, then estimated) with `formatMoney(parseAmount(remaining_amount), currency)` labels — Incomes + Actuals blocks render "coming next" placeholders for UC-07/08. Direct visits to the workspace touch the resume cookie via a tiny `MonthTouchClient` (sets `document.cookie` after mount) because Next 16 also forbids `cookies().set()` from RSC bodies; the cookie is a UX hint, not a security boundary (PRD §5.4). Out-of-range year/month route segments surface as 404 (range validation lives in the page's RSC guard). PRD §15 #3 + #4 + #18 covered by `tests/integration/months.test.ts` (10 cases — empty create, duplicate rejection, full clone snapshot for committed + estimated, inactive templates excluded from clone, no-cross-month sync, no-cross-tenant leakage, atomicity on duplicate, sorted list, month-not-found, cross-month no-sync) and `tests/e2e/months.spec.ts` (chromium + mobile-safari, 6 cases — empty-state → form create → cloned lines → cookie resume → second month newest-first list view → duplicate inline error → Spanish variant). |
| UC-07 | Month incomes | DONE | 2026-08-23 | Add / edit / hard-delete of month-scoped income rows (PRD UC-09 / §6.5 / C15 / §7.8). New `userId`-first repository `src/server/repositories/income.ts` (`insertIncome` / `findIncomeById` / `updateIncome` / `deleteIncome` / `listIncomesForMonth`); `month_income` has no direct `user_id` column so every read/write joins on `month` with `month.user_id = userId` (PRD §5.1). `update` uses PG `UPDATE ... FROM month WHERE month.user_id = userId` in one statement; `delete` uses a raw `sql` template (Drizzle's PG delete builder doesn't expose `.using()`) so the WHERE clause enforces tenancy in a single statement — PRD C15 / §13 hard delete on month-scoped money rows. `addMonthWorkspace` already calls the repo's `listMonthIncomes` so the workspace RSC ships with the existing rows. New service `src/server/services/incomes.ts` owns `addIncome` / `editIncome` / `deleteIncome` / `listIncomesForMonth`. Domain rules: the target category must be an ACTIVE income-kind row (ExpenseCategoryError / InactiveCategoryError / CategoryNotFoundError — the latter is reused from the categories service so the action layer maps one i18n key across both surfaces); the parent month is resolved via `findMonthById` so a missing month surfaces as `MonthNotFoundError` BEFORE the user picks a category (PRD §5.1). Money round-trips through `parseAmount` → integer cents → `formatCents` per ARCH §8 / ADR-5; negative amounts allowed (PRD UC-16). Hard delete on every remove (PRD C15); the `null` return from the repo (row not found / wrong tenant) maps to `IncomeNotFoundError`. Server actions `addIncomeAction` / `editIncomeAction` / `deleteIncomeAction` in `src/actions/incomes.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath` pattern (ARCH §5 rule 3). The actions carry the active `(year, month)` from the caller's URL so revalidation targets the exact workspace route the user is on (rather than every locale/month combo). Domain errors translate into `IncomeActionError = "expenseCategory" | "inactiveCategory" | "categoryNotFound" | "incomeNotFound" | "monthNotFound" | "amountFormat" | "validation"`. The UI replaces the UC-06 "coming next" placeholder with a real `IncomesScreen` client (`src/app/[locale]/months/[year]/[month]/incomes-screen.tsx`) — receives `initialIncomes` (serialized with `amountCents` integer cents, ADR-5 / ARCH §8) + ACTIVE income categories for the picker + the full category map for historical lookups (so a row whose category was later deactivated still shows its name + an inactive-note caption). The row toggles into an inline edit mode (`centsToInputString` seeds the AmountInput) and offers Delete with a `window.confirm` prompt. New helper `centsToInputString(cents)` in `src/i18n/format.ts` is the inverse of `formatMoney` for the integer-cents part so the edit form rehydrates the wire string from the stored cents. The RSC also reads the FULL income category list (`listCategoriesForManagement`) so it can label historical rows whose category is now inactive — verified by `tests/integration/incomes.test.ts` #8 (historical incomes keep displaying after the category is deactivated, PRD §6.5). PRD §15 #5 covered by `tests/integration/incomes.test.ts` (11 cases — happy create, negative amount, expense category rejected, inactive category rejected, unknown category rejected, edit replaces all fields, hard delete + row gone, edit/delete on missing row, historical income keeps rendering after category deactivate, two-tenant isolation including cross-tenant edit/delete, income CRUD never touches the template table) and `tests/e2e/incomes.spec.ts` (chromium + mobile-safari, 6 cases — add → edit → delete happy path with DB row-count check, no-active-categories empty state, Spanish variant with `Añadir`). |
| UC-08 | Actual expenses | DONE | 2026-08-23 | Real expense tickets (PRD UC-10 / UC-16; §6.7; C13 / C15; §7.2 / §7.3). New `userId`-first repository `src/server/repositories/actual.ts` (`insertActual` / `findActualById` / `updateActual` / `deleteActual` / `listActualsForMonthForUser`); `month_actual_expense` has no direct `user_id` column so every read/write joins on `month` with `month.user_id = userId` (PRD §5.1) — same tenancy pattern as the income repo. `update` uses PG `UPDATE ... FROM month WHERE month.user_id = userId` in one statement; `delete` uses a raw `sql` template (Drizzle's PG delete builder doesn't expose `.using()`) so the WHERE clause enforces tenancy in a single statement — PRD C15 / §13 hard delete on month-scoped money rows. The repo's `updateActual` patch type intentionally omits `edited_after_conversion` — the repo signature ENFORCES `edited_after_conversion = true` on every edit (PRD §7.5 undo gate) so the service cannot accidentally forget it; that flag is the input UC-10 keys off for "undo allowed only while unedited". New service `src/server/services/actuals.ts` owns `addActual` / `editActual` / `deleteActual` / `listActualsForMonth`. Domain rules: the target category must be an ACTIVE expense-kind row (IncomeCategoryError / InactiveCategoryError / CategoryNotFoundError — the latter reused from the categories service so the action layer maps one i18n key across both surfaces); the parent month is resolved via `findMonthById` BEFORE category validation so a missing month surfaces as `MonthNotFoundError` first (PRD §5.1). `observations` is OPTIONAL — the schema column is nullable and the service normalises empty/whitespace strings to `null` (PRD §6.7). Money round-trips through `parseAmount` → integer cents → `formatCents` per ARCH §8 / ADR-5; negative amounts allowed (PRD UC-16). Hard delete on every remove (PRD C15); the `null` return from the repo (row not found / wrong tenant / concurrent delete) maps to `ActualNotFoundError`. The service NEVER mutates `month_fixed_line.remaining_amount` — verified by `tests/integration/actuals.test.ts` #11 (snapshot of the reserved-line amounts is unchanged around two `addActual` calls). Server actions `addActualAction` / `editActualAction` / `deleteActualAction` in `src/actions/actuals.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath` pattern (ARCH §5 rule 3) and translate domain errors into `ActualActionError = "incomeCategory" | "inactiveCategory" | "categoryNotFound" | "actualNotFound" | "monthNotFound" | "amountFormat" | "validation"`. The `observations` field is `z.union([z.string().max(500), z.null()]).optional()` and the client uses a small `readObservations(formData)` helper to coerce `FormDataEntryValue` to a trimmed `string \| null`. The UI replaces the UC-06 "coming next" placeholder with a real `ActualsScreen` client (`src/app/[locale]/months/[year]/[month]/actuals-screen.tsx`) — receives `initialActuals` (serialized with `amountCents` integer cents, ADR-5 / ARCH §8) + ACTIVE expense categories for the picker + the full category map for historical lookups (so a row whose category was later deactivated still shows its name + an inactive-note caption). The add form carries a free-text `name`, an OPTIONAL `observations` input, the AmountInput, and the category select. Each row toggles into an inline edit mode (the AmountInput is seeded via `centsToInputString` and observations rehydrate to an empty string when `null` was stored). PRD §15 #11 + #14 + #15 covered by `tests/integration/actuals.test.ts` (16 cases — happy create, observations round-trip, negative amount, income category rejected, inactive category rejected, unknown category rejected, missing month rejected, edit sets `edited_after_conversion = true`, edit clears observations, hard delete + row gone, edit/delete on missing row, historical actual keeps rendering after category deactivate, no-envelope auto-balance (#14 + #15), two-tenant isolation including cross-tenant edit/delete, no template mutation, no income mutation) and `tests/e2e/actuals.spec.ts` (chromium + mobile-safari, 8 cases — add → edit → delete happy path with DB row-count check, no-active-categories empty state, no-envelope-mutation with reserved-line snapshot, Spanish variant with `Añadir`). All 151 Vitest + 40 Playwright e2e tests green; typecheck + lint clean. |
| UC-09 | Reserved lines (remaining, month-only) | DONE | 2026-08-23 | New `userId`-first repository `src/server/repositories/reserved-line.ts` (`insertMonthLine` / `findMonthLineById` / `updateMonthLineRemaining` / `deleteMonthLine`); `month_fixed_line` has no direct `user_id` column so every read/write joins on `month` with `month.user_id = userId` (PRD §5.1). `updateMonthLineRemaining` is a single-field patch on `remaining_amount` only — `original_amount`, `kind`, `origin`, `category_id`, `name` and `observations` are immutable for the life of the row (PRD §6.6); the repo signature enforces the rule so the service cannot accidentally rewrite history. `deleteMonthLine` uses a raw `sql` template (Drizzle's PG delete builder doesn't expose `.using()`) so the WHERE clause enforces tenancy in a single statement — PRD C15 / §13 hard delete on month-scoped money. New service `src/server/services/reserved-lines.ts` owns `updateRemainingAmount` / `addMonthOnlyLine` / `deleteMonthLine`. Domain rules: manual remaining edits are allowed for BOTH `committed` AND `estimated` kinds (PRD §6.6 / §7.4) and accept zero and negative values (PRD §7.4 / §7.6); month-only lines insert with `origin = 'month_only'` and `remaining_amount = original_amount = amount` (PRD §6.6) and the target category must be ACTIVE EXPENSE-kind (IncomeCategoryError / InactiveCategoryError / CategoryNotFoundError — the latter reused from the categories service so the action layer maps one i18n key across both surfaces). The parent month is resolved via `findMonthById` BEFORE category validation so a missing month surfaces as `MonthNotFoundError` first (PRD §5.1). Editing a remaining NEVER creates an actual ticket (PRD §7.3, UC-11) and NEVER mutates a template row (PRD §6.6 / §7.8) — implicit because the service only touches `month_fixed_line`. Server actions `updateRemainingAmountAction` / `addMonthOnlyLineAction` / `deleteMonthLineAction` in `src/actions/reserved-lines.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/months/<year>/<month>', 'page')` pattern (ARCH §5 rule 3) and translate domain errors into `ReservedLineActionError = "incomeCategory" | "inactiveCategory" | "categoryNotFound" | "monthLineNotFound" | "monthNotFound" | "amountFormat" | "validation"`. The UI replaces the UC-06 "coming next" placeholders with a real `ReservedLinesScreen` client (`src/app/[locale]/months/[year]/[month]/reserved-lines-screen.tsx`): committed/estimated sections in PRD §10 order, each row showing the current `remaining` with the cloned `original` underneath when they differ (so the manual edit is visible), an inline "Edit remaining" form bound to the `AmountInput`, an "X" delete with `window.confirm`, and an "Add month-only line" form (category + kind + name + amount) that shows a "create an active expense category first" message when the picker would be empty. Rows are pre-grouped by `kind` in the RSC and ordered with `cloned` before `month_only` so the snapshot reads as "what the template said" followed by "what I added on top" (PRD §7.8 / §6.6). The PRD §19 "Tickets do not reduce this number…" copy surfaces on the edit form so the manual-remaining affordance is obvious. i18n bundles extended with `reservedLines.*` + `validation.reservedLineNotFound`; parity test in `tests/unit/i18n-messages.test.ts` catches drift between locales. PRD scenarios #7 (groceries 400 → 350 raises savings to 800), #17 (one-off August line does not appear in September), #18 (August remaining edit does not leak into September), plus the §6.6 / §7.3 / §7.8 / C15 contract covered by `tests/integration/reserved-lines.test.ts` (17 cases). |
| UC-10 | Pass to actual | UC-10 | Pass to actual & undo | DONE | 2026-08-23 | undo | DONE | 2026-08-25 | New service `src/server/services/pass-to-actual.ts` owns `passToActual` + `undoPassToActual`. Each operation runs in ONE `db.transaction` (ARCH §5, PRD §16): pass inserts a new `month_actual_expense` (amount = line.remaining_amount, copied category_id / name / observations, `converted_from_line_id = line.id`, `converted_line_original_amount = line.original_amount`, `converted_line_origin = line.origin`, `edited_after_conversion = false`) and HARD-deletes the source line in the same statement batch — so the money exists in exactly one place after the move (PRD §7.2 / #8). Estimated lines throw `EstimatedLineCannotPassError` and the tx never opens (PRD §7.5 / #12). Undo is gated on `converted_from_line_id IS NOT NULL AND edited_after_conversion = false` (PRD §7.5 / #9 / #10): a re-insert of `month_fixed_line` REUSES `converted_from_line_id` as the row id (the row was hard-deleted, so the id is free) with `remaining_amount = actual.amount`, `original_amount = converted_line_original_amount`, `kind = 'committed'`, `origin = converted_line_origin`; the actual is then hard-deleted in the same tx. Both ops join on `month.user_id` for tenancy (PRD §5.1) and NEVER mutate a `template` row (PRD §7.8). New server actions `passToActualAction` / `undoPassToActualAction` in `src/actions/pass-to-actual.ts` follow the Zod-parse → `requireUserId()` → service → `revalidatePath('/[locale]/months/<year>/<month>', 'page')` pattern (ARCH §5 rule 3) and translate domain errors into `PassToActualActionError = "monthLineNotFound" | "estimatedLineCannotPass" | "actualNotFound" | "notUndoable" | "undoForbiddenAfterEdit" | "validation"`. UI: `ReservedLinesScreen.RowActions` now renders a "Pass to actual" button ONLY when `row.kind === 'committed'` (estimated rows simply have no such affordance, matching the spec). `ActualsScreen.RowActions` renders "Undo pass" ONLY when `convertedFromLineId !== null && !editedAfterConversion`. The committed section's heading carries the PRD §19 help copy "Move this commitment to actual spend. Estimates cannot be moved." so the lack of a button on estimated lines is self-explanatory. i18n bundles extended with `reservedLines.actions.{passToActual, passToActualHelp, confirmPassToActual}`, `actuals.actions.{undoPass, confirmUndoPass}`, and `validation.{estimatedLineCannotPassToActual, cannotUndoPass, notUndoable}`; parity test in `tests/unit/i18n-messages.test.ts` catches drift between locales. PRD §15 #8, #9, #10, #12 + the atomicity contract + tenancy isolation covered by `tests/integration/pass-to-actual.test.ts` (17 cases — happy pass, negative remaining, observations, month-only committed, estimated rejection, missing-line rejection, cross-tenant rejection, atomic rollback on simulated insert failure, template untouched, undo happy path, undo after edit forbidden, undo on non-converted forbidden, undo missing actual, undo cross-tenant, undo atomic rollback on simulated insert failure, undo template untouched, full round-trip). E2E coverage in `tests/e2e/pass-to-actual.spec.ts` (chromium + mobile-safari, 3 cases — pass→undo round-trip with DB row-count check, no pass-to-actual button on estimated lines, Spanish variant with `Pasar a gastos reales` / `Deshacer pase`). |
| UC-11 | Summary, savings & warnings | DONE | 2026-08-23 | New service `src/server/services/summary.ts` owns the read-only summary algebra. `getMonthSummary(userId, monthId)` returns `{ incomesTotal, actualsTotal, reservedRemainingTotal, potentialSavings }` in INTEGER CENTS — `potential_savings = Σ incomes − (Σ actuals + Σ remaining_amount of fixed/estimated lines)` (PRD §7.1 / #5–#8 / #14). The three money rows are pulled with three parallel `db.select()` calls against `month_income.month_id` / `month_actual_expense.month_id` / `month_fixed_line.month_id` and summed via `parseAmount` + `sumCents` (ADR-5, ARCH §8). Negatives are first-class (PRD §7.6). `getOverspendWarnings(userId, monthId)` computes per-category overspend: LEFT = `SUM(amount)` of `month_actual_expense` grouped by category (one PG `SUM()` aggregate, returns one row per category that has any tickets); RIGHT = `Σ amount` of ACTIVE templates (committed + estimated) for that category — same total as Fixed Expenses → Distribution by category (reuses `listActiveTemplates` so the baseline matches the clone-source query UC-06 uses at month creation, PRD §7.4 vs §7.8). A category with NO active templates is excluded from warnings. Warn when `actualsTotal − estimatedTemplateTotal > 0`. `isPastMonth(year, month, now)` returns true whenever the open month is not the current calendar month (PRD §7.7 / C8 / UC-13); `now` is injected so the helper is unit-testable without monkey-patching `Date`. Tenancy: every read resolves `month.user_id = userId` via `findMonthById` BEFORE the aggregation runs (PRD §5.1); a tenant calling with a `monthId` they don't own gets `MonthNotFoundError`. New RSC components: `summary-block.tsx` (income / actuals / reserved / savings hero card), `overspend-badge.tsx` (inline notification with PRD §19 copy plus `{plan}` / `{overrun}` template args), `past-month-banner.tsx` (PRD §7.7 banner). Workspace page.tsx now loads `summary` + `overspendWarnings` in parallel with the existing workspace fetch via `Promise.all`, then conditionally renders the past-month banner (above the summary) and the summary block (below the header). The overspend warnings are passed into BOTH the reserved-lines screen and the actuals screen; each screen de-dupes by `categoryId` and renders one `OverspendBadge` per overspending category above its row list. The reserved-lines screen shows the badge inside the affected group (committed or estimated) so the warning stays attached to the category's reserved money; the actuals screen shows the badge above the ticket list (the warning is about tickets exceeding the plan). i18n bundles extended with `months.summary.{negativeNote, pastMonthBanner}` + `warnings.{overspendPlan, overspendOver}` in BOTH `en` and `es`; parity test in `tests/unit/i18n-messages.test.ts` catches drift. PRD §15 #5 (Mortgage 800 + Groceries 400 + Income 2000 → savings 800), #6 (Groceries ticket 50 → savings 750, double-count by design PRD §7.3), #7 (decrease Groceries remaining 400→350 after the #6 ticket → savings back to 800), #8 (pass mortgage to actual → savings STILL 800, money in exactly one place), #13 (past-month banner visible, edits still allowed), #14 (Actual −20 raises savings by 20), #19 (Food templates 400+50, actuals 500 → warning; month remaining is IGNORED for the warning baseline; committed + estimated templates both count toward the plan; inactive templates are excluded from the baseline), plus tenancy isolation + hard-delete exclusion (PRD C15) + negative actuals algebra, all covered by `tests/integration/summary.test.ts` (19 cases) and `tests/e2e/summary.spec.ts` (6 cases — #5, #8, #19, no-warning edge, #13 past-month, Spanish variant). |
| UC-12 | PWA install | DONE | 2026-08-23 | Serwist service worker (`src/app/sw.ts`) now uses `runtimeCaching: []` — precache app shell only, no runtime caching of API/data responses (PRD C11, ARCH §7 / ADR-7). `src/app/manifest.ts` updated with `start_url` / `scope` from `NEXT_PUBLIC_APP_URL` (falls back to `https://expenses.jmsola.dev`), `display: 'standalone'`, icons 192 / 512 / maskable-512 in `public/icons/` (solid-color PNG placeholders generated at build time — swap for real artwork before production). New client component `src/components/pwa-install-prompt.tsx` implements the permanent install affordance (PRD UC-04): detects standalone mode via `display-mode: standalone` media query + iOS `navigator.standalone`, listens for `beforeinstallprompt` on Android Chrome to surface an install button, and shows iOS-specific "Add to Home Screen" instructions on Safari. Dismissable via an X button; hidden once installed. i18n keys expanded under `pwa.install.*` (button / prompt / iosInstructions / dismiss) in both `en.json` and `es.json`; `pwa.offlineNote` updated to match PRD C11 language. Affordance rendered on the home page (`src/app/[locale]/page.tsx`) below the language switcher. Typecheck + lint clean; all unit tests green. |
| — | Theme preference | DONE | 2026-08-23 | Dark/light/auto theme support (post-MVP enhancement). New `theme_preference` enum (`auto` / `light` / `dark`) added to DBML and Drizzle schema; `profile_settings.theme` column defaults to `auto`. Migration `0001_wakeful_sebastian_shaw.sql` adds the enum and column. New `updateTheme` repository function and service with `InvalidThemeError` validation. New `updateThemeAction` server action. New `src/components/theme-provider.tsx` client component wraps the app, reads user preference, detects system preference via `prefers-color-scheme` media query, applies `dark` class to `<html>`, and persists resolved theme to cookie. Inline script in root layout prevents flash of wrong theme on load. Settings page now includes theme selector alongside currency. i18n keys added under `settings.theme*` in both locales. Typecheck + lint clean; all unit tests green. Note: Schema extended beyond single-migration rule per user request. |
| UC-13 | One-off month expenses (special occasions) | DONE | | UX flow only — reuses `addMonthOnlyLine` from UC-09; no new backend |
| UC-14 | Annuals (yearly expense reminders) | DONE | 2026-09-01 | Catalog at `/[locale]/annuals` + month-workspace reminders (`getAnnualReminders` by `charge_month`, any year). Schema: `annual` table (migration 0002) + optional `amount` (migration 0003). Soft delete; inactive rows raise no reminders. Already in desktop/mobile nav. Feature shipped in 0.2.0; tracker catch-up. PRD: **UC-20** / C19 (implementation slice stays UC-14). |
| UC-15 | Global Stats (multi-year household analysis) | DONE | 2026-09-01 | Read-only `/[locale]/stats` observatory (C16 reports). No schema change. Recharts only. Desktop: Stats after Home. Mobile: Home · Stats · Fixed · More · Settings; More lists Annuals, Categories, History. Stats width `max-w-6xl`. |
