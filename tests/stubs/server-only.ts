// Vitest stub for the `server-only` marker package.
// The real package throws when imported outside a React Server Component;
// in test runs we run in plain Node so the marker is a no-op.
export {};
